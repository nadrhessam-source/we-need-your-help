/* ============================================================
   WE NEED YOUR HELP — Frontend Configuration
   تمام تنظیمات و ثابت‌های فرانت‌اند اینجاست.
   برای تغییر محیط (dev/prod) فقط این فایل رو ویرایش کن.
   ============================================================ */

window.APP_CONFIG = {

    // ------------------------------------------------------------
    // آدرس API بک‌اند (Cloudflare Worker)
    // ------------------------------------------------------------
    // در محیط development: می‌تونی از worker محلی یا نسخه deploy شده استفاده کنی
    // در محیط production: دامنه‌ی نهایی خودت
    // ------------------------------------------------------------
    API_BASE_URL: "https://api.we-need-your-help.example.workers.dev",

    // ------------------------------------------------------------
    // فاصله زمانی polling برای بررسی وضعیت پرداخت (میلی‌ثانیه)
    // هر 10 ثانیه یک بار از سرور می‌پرسیم که آیا تراکنش تأیید شده یا نه
    // ------------------------------------------------------------
    PAYMENT_POLL_INTERVAL_MS: 10000,

    // ------------------------------------------------------------
    // حداکثر مدت زمان polling (میلی‌ثانیه)
    // بعد از 30 دقیقه polling متوقف میشه (کاربر می‌تونه صفحه رو refresh کنه)
    // ------------------------------------------------------------
    PAYMENT_POLL_TIMEOUT_MS: 30 * 60 * 1000,

    // ------------------------------------------------------------
    // حداقل و حداکثر مبلغ مجاز (دلار)
    // ------------------------------------------------------------
    MIN_AMOUNT: 1,
    MAX_AMOUNT: 1000000,

    // ------------------------------------------------------------
    // آدرس صفحه تشکر
    // ------------------------------------------------------------
    THANKYOU_PATH: "thankyou.html",

    // ------------------------------------------------------------
    // تاریخ راه‌اندازی پروژه (برای محاسبه Days Since Launch)
    // فرمت: YYYY-MM-DD
    // ------------------------------------------------------------
    // این تاریخ در بک‌اند هم ذخیره میشه، این مقدار فقط fallback هست
    LAUNCH_DATE: "2026-01-01",

    // ------------------------------------------------------------
    // شبکه‌های پشتیبانی‌شده — نام نمایشی هر شبکه
    // ------------------------------------------------------------
    NETWORK_DISPLAY_NAMES: {
        polygon: "Polygon",
        base: "Base",
        ethereum: "Ethereum",
        tron: "TRON"
    },

    // ------------------------------------------------------------
    // حالت debug — اگه true باشه، خطاها در console نمایش داده میشن
    // در production این رو false کن
    // ------------------------------------------------------------
    DEBUG: true
};
